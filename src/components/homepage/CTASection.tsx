// The current design (index.html handoff) does not include a standalone CTA section —
// the call-to-action is embedded in the hero and the footer handles contact.
// This component is kept for CMS compatibility but renders nothing on the homepage.
export default function CTASection() {
  return null
}
